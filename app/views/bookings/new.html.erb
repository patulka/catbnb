<div class="container">
  <div class="booking-form">
    <h1>Take your kitty home</h1>
    <%= simple_form_for [@cat, @booking] do |f| %>
      <%= f.input :date_from %>
      <%= f.input :date_to %>
      <%= f.submit class: 'btn yellow-button' %>
    <% end %>
  </div>
  <div class="links-to-pages">
    <%= link_to 'Back to all cats', cats_path, class: "btn btn-light" %>
  </div>
</div>
