<h1>Cats, cats, cats...</h1>

  <div class="cards">

    <% @cats.each do |cat| %>
      <div class="card">
        <%= image_tag("#{cat.picture_url}") %>
        <div class="cat-info-box">
          <p><%= link_to cat.name, cat_path(cat) %></p>
          <p><%= cat.breed %> (<%= cat.color %> color)</p>
          <p class="btn btn-primary"><%= link_to "Rent me!", cat_path(cat) %></p>
        </div>
      </div>
    <% end %>

  </div>

